describe('returnContactData (c)', () => {
    it('should return status 200 and all the contact data with a registered number and address', () => {
        
    });
    it('should return status 206 and the partial contact data, with a registered number but unregistered address', () => {
        
    });
    it('should return status 404 with a register that doesn´t exists', () => {
        
    });

    it('should return status 500 and a error', () => {
        
    });
});